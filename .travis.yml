language: python
python:
  - "2.7"
  - "3.5"
  - "3.6"
cache: pip
install:
  - cd hello-world && pip install cython pytest
script:
  - cd hello-world && py.test --cov-report term-missing --cov app -v
